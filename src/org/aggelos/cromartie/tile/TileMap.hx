package org.aggelos.cromartie.tile;

class TileMap {

    var map:Array = new Array();
    
    function tileAt(x:Int,y:Int):Tile {
        
    }
    
    function getRow(rowNum:Int):Array {
        return map[rowNum];
    }
    
    function getColumn(colNum:Int):Array {
        var toRet:Array = new Array();
    }
  
}